# Install with:
#   pip install -r requirements.txt
#
# If you are using the same CUDA build as the current project environment,
# keep the PyTorch wheel index below.
--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.9.0+cu130
torchvision==0.24.0+cu130

numpy==2.2.6
pandas==2.3.3
matplotlib==3.10.7
pillow==11.3.0
tqdm==4.67.1
requests==2.33.1
pycocotools==2.0.11

# Selective Search in utils/selective_search.py uses cv2.ximgproc,
# which requires the contrib build instead of plain opencv-python.
opencv-contrib-python==4.12.0.88
